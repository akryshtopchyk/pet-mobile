import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yanovminsk',
  appName: 'YanovMinsk',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
